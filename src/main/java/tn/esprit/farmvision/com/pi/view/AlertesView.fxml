<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.Insets?>

<VBox spacing="15" style="-fx-padding: 20; -fx-background-color: #f8f9fa;"
      xmlns="http://javafx.com/javafx/17.0.2-ea"
      xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="com.pi.controller.AlertesController"
      prefWidth="400" prefHeight="500">

    <padding>
        <Insets top="20" right="20" bottom="20" left="20"/>
    </padding>

    <!-- Header -->
    <HBox spacing="10" alignment="CENTER_LEFT">
        <Label text="🔔 Centre de notifications"
               style="-fx-font-size: 18px; -fx-font-weight: bold; -fx-text-fill: #2c3e50;"/>
        <Region HBox.hgrow="ALWAYS"/>
        <Label fx:id="compteurAlertes" text="0"
               style="-fx-background-color: #e74c3c; -fx-text-fill: white; -fx-padding: 2 8; -fx-background-radius: 10;"/>
        <Button text="🔄" onAction="#rafraichir"
                style="-fx-background-color: transparent; -fx-cursor: hand;"/>
    </HBox>

    <!-- Filtres rapides -->
    <HBox spacing="10">
        <Button text="Toutes" onAction="#filtrerToutes"
                style="-fx-background-color: #3498db; -fx-text-fill: white;"/>
        <Button text="⚠️ Urgentes" onAction="#filtrerUrgentes"
                style="-fx-background-color: #e74c3c; -fx-text-fill: white;"/>
        <Button text="🔧 Maintenances" onAction="#filtrerMaintenances"
                style="-fx-background-color: #f39c12; -fx-text-fill: white;"/>
    </HBox>

    <!-- Liste des alertes -->
    <ListView fx:id="alertesListView" VBox.vgrow="ALWAYS"/>

    <!-- Bouton tout marquer comme lu -->
    <Button text="✓ Tout marquer comme lu" onAction="#toutMarquerLu"
            style="-fx-background-color: #27ae60; -fx-text-fill: white; -fx-padding: 10;"/>
</VBox>